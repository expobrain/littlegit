language: python

cache: pip

matrix:
    include:
        -   python: 2.7
        -   python: 3.4
        -   python: 3.5
        -   python: 3.6
        -   python: 3.7
            sudo: required
            dist: xenial

sudo: required

install:
    - pip install -e .

before_script:
    - pip install -r requirements-dev.txt

script:
    - pytest -x tests.py
